export default {
   serverURL: 'https://chainstory.art/api',
   //serverURL: 'http://localhost:3001',
   donationLowerBound: Math.pow(10, 8) * 2, // The amount considered as a donation, golden link

   image: {
      width: 1024,
      tnum: 64
   }
};
